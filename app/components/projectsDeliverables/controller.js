angular
    .module('riskApp')
    .controller('ProjectsDeliverablesController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsDeliverablesController init");

        }
    ]);
