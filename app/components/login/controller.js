angular
    .module('riskApp')
    .controller('LoginController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsDeliverablesController init");

        }
    ]);
