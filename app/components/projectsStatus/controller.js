angular
    .module('riskApp')
    .controller('ProjectsStatusController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsStatusController init");

        }
    ]);
