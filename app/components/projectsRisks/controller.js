angular
    .module('riskApp')
    .controller('ProjectsRisksController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsRisksController init");

        }
    ]);
