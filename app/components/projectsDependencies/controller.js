angular
    .module('riskApp')
    .controller('ProjectsOverviewController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsOverviewController init");

        }
    ]);
