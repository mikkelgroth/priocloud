angular
    .module('riskApp')
    .controller('ProjectsDependenciesController', [
        '$scope', 
        '$rootScope',
        function (
            $scope, 
            $rootScope
        ) {

            console.log("ProjectsDependenciesController init");

        }
    ]);
